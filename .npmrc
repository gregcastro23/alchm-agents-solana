# Don't fail on peer dependency warnings
strict-peer-dependencies=false

# Use specific registry
registry=https://registry.yarnpkg.com

# Allow scripts but with timeout to prevent hangs
script-shell=/bin/bash
